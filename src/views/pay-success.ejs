<%- include('layout', { section: 'head', title: 'Payment received', currentNav: null, session: session }) %>

<h1>Payment received</h1>
<p>Thanks — your registration is being activated. This usually takes a few seconds.</p>
<p>If your account doesn't show as paid within a minute, refresh the dashboard.</p>
<p><a href="/dashboard">Go to dashboard</a></p>

<%- include('layout', { section: 'foot' }) %>
