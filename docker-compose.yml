version: '3.9'
services:
  tcladdr:
    build: .
    container_name: tcladdr
    restart: unless-stopped
    ports:
      - "127.0.0.1:3500:3500"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
